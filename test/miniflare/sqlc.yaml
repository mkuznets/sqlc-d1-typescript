version: "2"
plugins:
  - name: d1-ts
    wasm:
      url: file:///candidate-required/plugin.wasm
sql:
  - schema: "schema.sql"
    queries: "queries.sql"
    engine: "sqlite"
    codegen:
      - plugin: d1-ts
        out: src
        options:
          interface: workers
